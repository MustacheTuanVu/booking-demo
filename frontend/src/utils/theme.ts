import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: `"Roboto Condensed", "Roboto Condensed Fallback", sans-serif !important`,
  },
});

export default theme;